/**
 * Resultado de un diagnóstico cognitivo generado por un LLM vía MCP tool calling.
 *
 * Re-exportado desde {@link application/use-cases/executeCognitiveDiagnosis}
 * para mantener compatibilidad con los imports existentes. El tipo vive en la
 * capa de aplicación (clean architecture: application no importa de infrastructure).
 */
export { type CognitiveDiagnosisResult } from '@/application/use-cases/executeCognitiveDiagnosis.js'
