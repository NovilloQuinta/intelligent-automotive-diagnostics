import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.iad.dashboard',
  appName: 'Intelligent Automotive Diagnostics',
  webDir: 'dist'
};

export default config;
